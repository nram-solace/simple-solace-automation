[solace]
local_docker mgmt_host=127.0.0.1 mgmt_port=8080 semp_username=admin semp_password=admin

[local]
local_docker mgmt_host=127.0.0.1 mgmt_port=8080 semp_username=admin semp_password=admin

[remote]
local_ubuntu mgmt_host=nram-latitude mgmt_port=8080 semp_username=admin semp_password=admin

# Do NOT Edit the below lines!!
[solace:vars]
ansible_connection=local
